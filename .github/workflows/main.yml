name: Build GNOME Extension
on:
  release:
    types: [published]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2
      - name: Set up GNOME Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y gnome-shell-extensions
      - name: Build GNOME Extension
        run: |
          cd github-actions@arononak.github.io
          make build
      - name: Upload Extension
        uses: actions/upload-artifact@v2
        with:
          name: github-actions@arononak.github.io.shell-extension.zip
          path: github-actions@arononak.github.io/github-actions@arononak.github.io.shell-extension.zip
